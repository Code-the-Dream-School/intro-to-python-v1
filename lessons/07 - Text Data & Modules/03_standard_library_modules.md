# Standard Library Modules: csv, os, datetime

> **Source:** Ported from Python Essentials — PE 2.3 (Introduction to Modules) and PE 2.5 (Working with the OS)
>
> **Porting notes:** Take PE 2.3 import syntax (import, from...import, aliases) and PE 2.5 `os` module content. `csv` is already covered in the previous section — this section frames it as part of the standard library and deepens the understanding. Focus the `os` section on the most practical functions: `os.getcwd()`, `os.path.exists()`, `os.path.join()` (for building paths that work cross-platform), and `os.listdir()`. Add `datetime` as a new example not in PE — basic date creation, formatting with `.strftime()`, and `datetime.now()`. Introduce Python docs navigation as a skill.

TBD
